log_diaper_change:
  name: Log diaper change
  description: Log one diaper change for a Baby Diary baby.
  fields:
    type:
      name: Type
      description: The type of diaper change.
      required: true
      selector:
        select:
          options:
            - xixi
            - coco
            - ambos
    entry_id:
      name: Entry ID
      description: Optional Baby Diary config entry ID. Only needed when tracking multiple babies.
      required: false
      selector:
        text:

